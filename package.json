{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "changeset": "changeset",
    "ci:version": "changeset version",
    "ci:publish": "pnpm publish -r"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.8",
    "typescript": "^5.5.4"
  }
}